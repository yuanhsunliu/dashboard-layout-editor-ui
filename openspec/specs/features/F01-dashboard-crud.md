# F01: Dashboard CRUD

## Overview
使用者可以建立、檢視、編輯、刪除 Dashboard，並從列表中選擇進入編輯。

## User Stories
- 作為使用者，我可以查看所有 Dashboard 列表，以便選擇要編輯的 Dashboard
- 作為使用者，我可以建立新的 Dashboard，以便開始設計報表
- 作為使用者，我可以重新命名 Dashboard，以便識別不同報表
- 作為使用者，我可以刪除不需要的 Dashboard，以便保持列表整潔
- 作為使用者，我可以點擊 Dashboard 進入編輯頁面

## Acceptance Criteria

### Dashboard 列表
- [ ] 進入首頁顯示 Dashboard 列表
- [ ] 列表顯示 Dashboard 名稱與建立/更新時間
- [ ] 空狀態顯示引導建立第一個 Dashboard

### 建立 Dashboard
- [ ] 點擊「新增 Dashboard」按鈕可建立
- [ ] 新 Dashboard 預設名稱為「未命名 Dashboard」
- [ ] 建立後自動導向該 Dashboard 編輯頁

### 重新命名
- [ ] 可在列表中 inline edit 修改名稱
- [ ] 或在編輯頁 Header 修改名稱
- [ ] 名稱不可為空

### 刪除 Dashboard
- [ ] 點擊刪除按鈕顯示確認 Dialog
- [ ] 確認後刪除並更新列表
- [ ] 刪除成功顯示 Toast 通知

### 進入編輯
- [ ] 點擊 Dashboard 卡片/項目進入編輯頁
- [ ] URL 更新為 `/dashboard/:id`

## UI/UX Spec

### Dashboard 列表頁 (`/`)
```
┌─────────────────────────────────────────┐
│  Dashboard Layout Editor    [+ 新增]    │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────┐  ┌─────────────┐       │
│  │ 銷售報表    │  │ 行銷分析    │       │
│  │ 2024/01/01  │  │ 2024/01/02  │       │
│  │      [編輯名稱] [刪除]       │       │
│  └─────────────┘  └─────────────┘       │
│                                         │
└─────────────────────────────────────────┘
```

### 空狀態
```
┌─────────────────────────────────────────┐
│                                         │
│        📊 尚無 Dashboard                │
│                                         │
│     [建立第一個 Dashboard]              │
│                                         │
└─────────────────────────────────────────┘
```

### 刪除確認 Dialog
- 標題：「刪除 Dashboard」
- 內容：「確定要刪除「{name}」嗎？此操作無法復原。」
- 按鈕：[取消] [刪除]（destructive style）

## Data Model

```typescript
interface Dashboard {
  id: string;
  name: string;
  createdAt: string;  // ISO 8601
  updatedAt: string;  // ISO 8601
  layout: LayoutItem[];
  widgets: Widget[];
  theme: 'light' | 'dark';
}

interface DashboardListItem {
  id: string;
  name: string;
  createdAt: string;
  updatedAt: string;
}
```

## API Contract

### GET /api/dashboards
列出所有 Dashboard

**Response:**
```json
{
  "dashboards": [
    {
      "id": "dashboard-1",
      "name": "銷售報表",
      "createdAt": "2024-01-01T00:00:00Z",
      "updatedAt": "2024-01-02T00:00:00Z"
    }
  ]
}
```

### POST /api/dashboards
建立新 Dashboard

**Request:**
```json
{
  "name": "未命名 Dashboard"
}
```

**Response:**
```json
{
  "id": "dashboard-2",
  "name": "未命名 Dashboard",
  "createdAt": "2024-01-03T00:00:00Z",
  "updatedAt": "2024-01-03T00:00:00Z"
}
```

### PATCH /api/dashboards/:id
更新 Dashboard（重新命名）

**Request:**
```json
{
  "name": "新名稱"
}
```

### DELETE /api/dashboards/:id
刪除 Dashboard

**Response:** 204 No Content

## Dependencies
- T01: Project Setup

## Out of Scope
- Dashboard 複製功能
- Dashboard 排序/搜尋/篩選
- Dashboard 分類/標籤
