# F08: i18n (Internationalization)

## Overview
支援多語系，讓使用者可以切換介面語言。

## User Stories
- 作為使用者，我可以使用繁體中文介面
- 作為使用者，我可以切換至英文介面
- 作為使用者，語言偏好會被記住

## Acceptance Criteria

### 語言支援
- [ ] 支援繁體中文 (zh-TW) - 預設
- [ ] 支援英文 (en)

### 語言切換
- [ ] 在 Header 或 Settings 提供語言切換
- [ ] 切換後立即套用，無需重新整理

### 語言偏好
- [ ] 語言偏好儲存在 localStorage
- [ ] 頁面載入時讀取並套用偏好
- [ ] 預設使用瀏覽器語言設定

### 翻譯覆蓋範圍
- [ ] 所有 UI 文字（按鈕、標題、提示）
- [ ] 錯誤訊息
- [ ] 表單驗證訊息

## UI/UX Spec

### 語言切換
```
Header 或 Footer:
[繁中 ▼]
├── 繁體中文
└── English
```

或使用 Icon:
```
[🌐] → 切換語言選單
```

## Directory Structure

```
src/locales/
├── zh-TW/
│   ├── common.json      # 通用翻譯
│   ├── dashboard.json   # Dashboard 相關
│   ├── chart.json       # 圖表相關
│   └── validation.json  # 驗證訊息
├── en/
│   ├── common.json
│   ├── dashboard.json
│   ├── chart.json
│   └── validation.json
└── index.ts             # i18n 設定
```

## Translation Files

### common.json
```json
{
  "zh-TW": {
    "common": {
      "save": "儲存",
      "cancel": "取消",
      "delete": "刪除",
      "confirm": "確認",
      "retry": "重試",
      "loading": "載入中...",
      "error": "發生錯誤"
    }
  },
  "en": {
    "common": {
      "save": "Save",
      "cancel": "Cancel",
      "delete": "Delete",
      "confirm": "Confirm",
      "retry": "Retry",
      "loading": "Loading...",
      "error": "An error occurred"
    }
  }
}
```

### dashboard.json
```json
{
  "zh-TW": {
    "dashboard": {
      "title": "Dashboard 列表",
      "create": "新增 Dashboard",
      "unnamed": "未命名 Dashboard",
      "deleteConfirm": "確定要刪除「{{name}}」嗎？此操作無法復原。",
      "saveSuccess": "儲存成功",
      "saveFailed": "儲存失敗"
    }
  },
  "en": {
    "dashboard": {
      "title": "Dashboard List",
      "create": "Create Dashboard",
      "unnamed": "Untitled Dashboard",
      "deleteConfirm": "Are you sure you want to delete \"{{name}}\"? This action cannot be undone.",
      "saveSuccess": "Saved successfully",
      "saveFailed": "Failed to save"
    }
  }
}
```

### chart.json
```json
{
  "zh-TW": {
    "chart": {
      "config": "圖表設定",
      "type": "圖表類型",
      "dataSource": "資料來源",
      "xAxis": "X 軸欄位",
      "yAxis": "Y 軸欄位",
      "preview": "預覽",
      "types": {
        "line": "折線圖",
        "bar": "長條圖",
        "pie": "圓餅圖"
      }
    }
  },
  "en": {
    "chart": {
      "config": "Chart Configuration",
      "type": "Chart Type",
      "dataSource": "Data Source",
      "xAxis": "X-Axis Field",
      "yAxis": "Y-Axis Field",
      "preview": "Preview",
      "types": {
        "line": "Line Chart",
        "bar": "Bar Chart",
        "pie": "Pie Chart"
      }
    }
  }
}
```

## i18n Setup

```typescript
// src/locales/index.ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';

import zhTWCommon from './zh-TW/common.json';
import zhTWDashboard from './zh-TW/dashboard.json';
import zhTWChart from './zh-TW/chart.json';
import enCommon from './en/common.json';
import enDashboard from './en/dashboard.json';
import enChart from './en/chart.json';

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      'zh-TW': {
        common: zhTWCommon,
        dashboard: zhTWDashboard,
        chart: zhTWChart,
      },
      en: {
        common: enCommon,
        dashboard: enDashboard,
        chart: enChart,
      },
    },
    fallbackLng: 'zh-TW',
    defaultNS: 'common',
    interpolation: {
      escapeValue: false,
    },
  });

export default i18n;
```

## Usage

```typescript
import { useTranslation } from 'react-i18next';

function DashboardHeader() {
  const { t } = useTranslation('dashboard');
  
  return (
    <h1>{t('title')}</h1>
    <Button>{t('create')}</Button>
  );
}

// 帶參數
function DeleteDialog({ name }: { name: string }) {
  const { t } = useTranslation('dashboard');
  
  return (
    <p>{t('deleteConfirm', { name })}</p>
  );
}
```

## Dependencies
- T01: Project Setup

## Out of Scope
- 日期/數字格式本地化
- RTL (右到左) 語言支援
- 動態載入語言包
- 翻譯管理後台
